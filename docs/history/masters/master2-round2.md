# MASTER 2 (THREE.JS VISUALIZATION) — Round 2

## 1. CRITIQUES

### vs M1 (orbital/geophysics)

- **B1 version pin — M1 must concede.** M1's entire deliverable is hand-rolled math (mulberry32, Box-Muller, bilinear, Catmull-Rom, CW). It has **zero three.js API surface**. M2's pin is the only one tied to actual rendering behavior (tone-mapping/color-space output, `renderer.info` budget semantics, OrbitControls + BufferGeometryUtils addon APIs). M1's 0.170.0 is arbitrary preference; M2's 0.186.0 stands because nothing in M1's work can break from it. What breaks from rendering standpoint if 0.170.0 wins: nothing catastrophic — the addons I use (OrbitControls, BufferGeometryUtils.mergeGeometries) are API-stable across that range — but there is no rendering reason to prefer the older pin, and pinning latest-stable at build maximizes the support window for a shipped static page.
- **B8 S2 grid — renderable either way, but pick one and freeze the formula.** 41² vs 81² is 1,681 vs 6,561 LUT entries; my amber player line samples a 512-pt conic family — the resolution difference is invisible in the render. What I need from M1 that is currently missing: the exact LUT API contract (`flyby.sample(aimDx, aimDy) → {rp, path: Float32Array}`), a **formula version tag** burned into every still sidecar (LUT is runtime-generated from a seeded formula; if the formula revs, stills pixels change and "deterministic frames" is a lie without the tag), and confirmation the LUT stays CPU-side (my amber line needs CPU sampling; keep it out of GPU textures).
- **B2/B3 TRUST/TIME — meters are scale-agnostic given one normalization contract.** Nothing breaks in WebGL. What's missing: **writer ownership**. M1 says derived values recomputed by single `recompute(STEAL)`; M3 has an earn/spend matrix with its own actions. If both write `trust`/`time_left`, the ledger forks. Demand: M1's `recompute()` is the **sole writer** of derived resource fields; M3's actions mutate inputs then call it. Exact fields needed: `trust` (0..100 float), `timeDays` (consumed counter from 0), `timeContingency` (120), `dv.reserve/spent/ledger`.
- **Missing render-layer inputs (exact data):** (a) knot-table schema from canon.json, incl. the `display` block field names (`R0`, `R_ref`) my log2 transform reads; (b) per-link Saturn-local frames from chain.json (`linkIndex → {targetMoon, encounterT, localFrame}`) for billiards staging; (c) core.json zone schema (`[{rInner, rOuter, mineral, color}]`) for zoned vertex colors; (d) `s4.computeP(STEAL) → {p, tag}` — M1 computes probability, I only map p→cloud alpha/density; the mapping function must be specified, not improvised by me; (e) freeze contract: my accumulator pauses sim-time while RAF continues — M3's input-lock must ride the same event (`sim/freeze/start|end`), not a second timer; (f) `seed: 0x5TEA1` is invalid hex and must not ship — replace with a real uint32.
- **Contradicts canon:** nothing in M1's models contradicts canon sim definitions; the S4 "honestly a response-surface mapping, not n-body" disclosure is exactly right and my ghost-cloud shader treats it as such (per-ghost offsets, no dynamics claims).

### vs M3 (interaction/game design)

- **30 screens vs 6 scene modes (B5, B11) — the mapping table is the missing contract.** My persistent scene has 6 mode groups; M3 has 30 screens. That's fine — screens ≠ modes; most story screens are DOM-only or reuse a mode — but M3's spine is S-1→S-10 (10 chapters) against canon's 11 scenes, so I cannot build a screen→mode wiring table on my side. **Unbuildable in one persistent scene:** nothing, *provided* M3 delivers, per screen: `{sceneMode | 'dom-only', cameraRig, requiredWebGLState}`. Without it, `director.enter()` has no key to switch on. This is B11 and it is M3's (with M4's) to produce.
- **B6 mobile — I concede the interaction contract, and my Round 1 OrbitControls attachment was wrong.** M3/M5: canvas is display-only on mobile; all inputs are real DOM controls below it. Attaching OrbitControls on a display-only canvas is dead weight that risks touch scroll-hijack. Revised contract: OrbitControls attach only when `tier==='desktop'` (see §2). Consequence for my S2: the draggable aim marker becomes **desktop-only**; on mobile it is a DOM stepper + numeric field. This requires a new render-layer API — `SceneDirector.setModeState(mode, partial)` — a DOM-bindable state setter with **no canvas hit-testing required**. M3's screens must call it instead of reaching into the canvas. Exact DOM contract: `director.setModeState('flyby', {aimDx, aimDy})`, `director.setModeState('shepherd', {nudges:[...]})`, etc.
- **B9 S5 body count — M3 contradicts canon and I can't render both.** Canon sim-spec: 5–7 asteroids. M3's S5: "drag velocity arrows on **3** shepherd bodies." My Round 1 rendered 3. What's unbuildable: nothing — but I need a decision. My reconciliation: render 5–7 asteroid bodies (canon), of which **3 are the player-nudgeable shepherds** (M3's gameplay subset), the rest background field bodies. Needs M3/M1 signoff. Also missing numbers I cannot invent: **per-body nudge caps** (M3 says "capped per-body" with no values) and the **resonance band definition** (who computes in-band? — not the render layer).
- **Stills at commit time (B7/C9).** M3's COMMIT handlers (S2 COMMIT → Exhibit 42B, B-5 ACCEPT) need exhibit artifacts, but M3 never specifies who triggers capture. Missing DOM contract: M3 calls `exhibits.captureOnCommit(runId, stillId)` after each commit; my pipeline returns `{png, sidecar}`; M5's compositor finishes the plate. Without this hook, Exhibit 42B is a DOM table with no canvas evidence.
- **Contradicts canon:** M3's spine (S-1→S-10, billiards at S-4) diverges from canon scene order (scene 6 = THREE TRILLION DOORS = billiards shot planner; scene 8 = THE SLIP; scene 9 = EARTH, SOMEHOW). My CameraRig presets and mode-enter hooks are keyed by scene id — a 10-chapter spine against 11 canon scenes breaks the keying. C6 resolves this (11 scenes, S-8F = Scene 8 fail state), but M3 must realign its screen numbering, not me.
- **0.5s freeze:** M3 says "input lock, not timing-skill" — agreed, and my Round 1 already implements it as sim-time pause with RAF continuing. The only missing piece is the shared event name (see M1 critique (e)).

### vs M4 (storytelling)

- **B4 — M4 renames the sims; my render plans are built to canon.** S2 as "heist engine sim," S4 as "helium-3 worksheet," S3 as "cascade chain," S5 as "Slip window sim," S1 as "Mimas tour" — none of the worksheet/cascade/slip-window mechanics have a WebGL surface in my proposal, and **I will not build new render modes for them.** Blunt version: per C5, the worksheet, checksum button, hidden-burn toggle, and cascade chain are **DOM interactions** (DOM owns chrome — that's my architecture's rule, and it holds here). If any of them genuinely needs 3D, M4 must file a render-mode request with an exact geometry spec (meshes, transforms, state fields) — a paragraph of prose is not a spec.
- **Scene 7 "dossier wall, NO sim" and Scene 11 core console** are trivially renderable (dom-only screen; gCore mode + DOM meters). Scene 10's "staged as hearing room" is DOM set dressing — fine — but the hearing replays are **currently unrenderable**: M3 stores RUNs as decisions+seed, and nobody has specified `stateFromRun(run)` — the deterministic derivation from a RUN record to a per-mode state snapshot (`{mode, t, linkIndex, aimOffset, toggles…}`). Without it I cannot re-enter `director.enter('flyby', …)` for Exhibit 42B. This function is M1's to write, M4's scenes to call, mine to consume. It's the single biggest missing piece in the whole system.
- **FINAL CANON gold override breaks M5's truth grammar** unless it's an explicit exception. M3's FINAL CANON perk ("archive cyan replaced by gold" in S2) changes my S2 line colors at runtime keyed on `STEAL.declass === 'FINAL_CANON'`. My LinePool supports palette swap — buildable — but the rule must be exact and labeled ("CANON OVERRIDE — palette exception, see legend"), or the cyan=archive grammar silently lies.
- **Missing from M4:** the 11-scene→render-mode table (half of B11 — which mode or dom-only per scene, which camera preset); ARGUS's "+3s pause in S8" needs an event contract (who owns the timer — I propose the sim clock, surfaced as `sim/pause {seconds:3, reason}`); the L0 plate cover means the WebGL mode behind a locked plate should **not** enter — contract: declass-gated plates are `dom-only` until unlocked (saves GPU, avoids rendering content the player can't see).

### vs M5 (visual design)

- **B7/C9 stills seam — M5's composite and my capture must meet at one API.** M5: "FILE AS EXHIBIT" composites plate-frame DOM + canvas bitmap + SVG overlay into 1600×1000 PNG. My Round 1: `renderer.domElement.toDataURL` with `preserveDrawingBuffer` toggled during capture. The seam is exact: **my pipeline produces the canvas bitmap + sidecar; M5's compositor produces the final plate.** Contract: `StillCapture.capture(stillId) → {bitmap: ImageBitmap, sidecar: JSON}`; M5's `composePlate(bitmap, overlaySVG, furniture) → PNG`. Two open items: (a) M1 says "no build step" while C9 wants build-time PNGs — resolved in §2 by capturing in-browser on a QA page and **committing the PNGs** (no build step, checked-in artifacts); (b) filename contract — M5 specifies `exhibit-7b-plate-<nn>.png`; my sidecars must reference it exactly.
- **Color fidelity break: my ACES tone mapping vs M5's exact hexes.** M5 specifies OBSERVED cyan `#71A9AD` / PREDICTED amber `#C69A45` as WebGL line colors. My Round 1 ran everything through ACESFilmic, which **will not reproduce those hex values** — the truth grammar would be approximately right, which is exactly wrong for a forensic grammar. Fix (in §2): all truth-grammar line materials get `toneMapped: false`; bodies/clouds keep ACES. Also my "paper-tinted fog/clear color sampled from --paper" **contradicts M5's seam rule** ("canvas background must be transparent; never paint paper in GL"). Revised: `alpha: true`, clear alpha 0, no paper fog.
- **Truth-grammar dash params vs my log2 compression.** M5 gives `LineDashedMaterial` dashSize/gapSize in world units — but my trajectory modes use log2 radial compression, so a dash that reads correctly near Saturn reads wrong near Jupiter. What's missing is per-mode dash compensation, which answers my Round 1 open question (see §2): **yes, build the shared LineStyle utility.**
- **SVG STATIC plates (M5 §6):** consistent with my no-WebGL/reduced-motion tiers. Who draws them is the gap: M5 designs the plates; the projections must come from the same math (S2 side-by-side trajectories need M1's LUT sample; S4 scatter needs ghost offsets). Contract: `FallbackSVG.render(mode, stateSnapshot)` consumes the identical state snapshot my WebGL modes consume — one state schema, two renderers.
- **a11y contract:** M5 requires `role="img"` + `aria-label` per canvas. I need the label strings — propose a per-scene `a11y` field in scenes.json (M4/M5 to author, I render). Also: POV classes (`.pov-ren` etc.) sit on `.plate-frame` wrapping my canvas — they must never alter canvas pixels; that's M5's to enforce, and my transparent-canvas rule makes it hold.
- **`content-visibility: auto` on off-screen plates:** compatible with my `visible=false` groups; no conflict.

## 2. REVISION OF YOUR PROPOSAL

Changed sections only. Sections not listed are **UNCHANGED**.

- **§1 Scene architecture — REVISED (B6/C8 mobile contract).** OrbitControls now attach **only when `tier === 'desktop'`** (desktop = WebGL2 + width ≥ 1024 + no reduced-motion requirement on controls). On mobile/no-WebGL tiers, all five previously-orbit modes use fixed cinematic rigs; zero canvas gesture handlers; canvas is `pointer-events: none` except the S4 tap-tag hotspot which becomes a DOM list on mobile anyway. **New API (the DOM contract M3 needs):** `SceneDirector.setModeState(mode, partialState)` — DOM controls write sim state without touching the canvas (e.g. `setModeState('flyby', {aimDx, aimDy})`, `setModeState('shepherd', {nudges})`, `setModeState('timeline', {t})`). Canvas hit-testing (S2 aim drag, S5 tap impulse) is retained **desktop-only** as a progressive enhancement writing through the same function. Also new: `director.enter()` is a no-op for declass-gated plates (M4 contract — dom-only until unlocked), and the sim clock exposes `sim/pause {seconds, reason}` events (ARGUS +3s, freeze) that pause the fixed-step accumulator while RAF continues.
- **§2 Per-sim render — REVISED.** **S2:** aim-marker drag desktop-only; mobile = DOM steppers via `setModeState`; amber line recomputes from the same CPU LUT sample in both paths (identical pixels for identical state — stills-safe). **S5: body count 5–7 per canon (B9)** — 5–7 instanced asteroid bodies in the Mimas-centered frame; exactly 3 flagged `interactive` (the gameplay shepherds, M3's subset), remainder background field; pending M3/M1 signoff on the split and on nudge-cap numbers. **S4:** UNCHANGED (one THREE.Points, 1 draw call) + explicit rule: reduced-motion renders the cloud as a single static frame from the same buffer (no re-sim). **Billiards/core/atlas:** UNCHANGED, except truth-grammar lines now route through the shared LineStyle utility (below).
- **§3 Display-space mapping — REVISED (M5 seam + color fidelity).** Renderer: `alpha: true`, `setClearColor(0x000000, 0)` — **no paper-tinted fog, no painted ground** (M5's seam rule). ACESFilmic stays for bodies/clouds/shockwaves, but **all truth-grammar line materials set `toneMapped: false`** so `#71A9AD`/`#C69A45`/`#8E3434` render as specified. Log2 radial compression UNCHANGED; `R0`/`R_ref` field names to be confirmed from canon.json `display` block (M1 to supply exact names).
- **§4 Stills — REVISED (B7/C9).** Primary: build-time pre-rendered PNGs (M5's composite). My runtime pipeline becomes the **capture engine**: `StillCapture.capture(stillId) → {bitmap, sidecar}` (3 deterministic warm-up frames, seeded RNG, sidecar now includes **LUT formula version tag** + canon file hashes + `exhibit-7b-plate-<nn>.png` filename reference). `preserveDrawingBuffer` toggled only during capture — UNCHANGED. **No-build-step resolution (M1 vs M5):** capture runs in-browser on a QA page; PNGs + sidecars are **committed to `exhibits/`** as checked-in artifacts. No build step, no regeneration at runtime, re-capturable on demand. The "FILE AS EXHIBIT" button calls the same engine live, then M5's `composePlate`.
- **§5 Mobile/fallback tiers — REVISED (C8).** Mobile reduced tier: canvas display-only (no OrbitControls, no canvas gestures); S2/S5 SVG-DOM fallbacks UNCHANGED (now the *primary* mobile path, not a degradation); `FallbackSVG.render(mode, stateSnapshot)` shares the WebGL state schema. Frame governor UNCHANGED. Reduced-motion: instant cuts UNCHANGED; S5 STEP MODE is M3's interaction, my layer just renders each stepped state. No-WebGL: full DOM/SVG fallback UNCHANGED.
- **§6 OSS — REVISED (B1/C2): defend 0.186.0.** M1 concedes: his deliverable has zero three.js API surface, so the pin belongs to the rendering master. 0.186.0 = latest stable at build (r184 stable Apr 2026, r186 latest per Sept 2026 verification); OrbitControls and BufferGeometryUtils APIs I use are unchanged across 0.170→0.186, so nothing of M1's (or anyone's) breaks. Load via npm lockfile + jsdelivr importmap fallback — UNCHANGED. Addons list UNCHANGED (OrbitControls, BufferGeometryUtils only; no EffectComposer, no physics, no GLTF, no r3f).
- **Open question — shared LineStyle utility: YES, build it.** `LineStyle.js` (framework-free, importable by both the WebGL layer and the DOM/SVG tier) exports the truth-grammar table: for each of OBSERVED/PREDICTED/HYPOTHETICAL/DISPUTED — `{cssColor, cssDasharray, threeColor, threeDash: {dashSize, gapSize}, toneMapped: false}` with M5's exact values (solid cyan-deep/`#71A9AD`; dashed amber-deep/`#C69A45` `8 5` / dashSize .35 gapSize .22; dotted slate 60% `1.5 5` round / dashSize .02 gapSize .18; disputed double red `#8E3434` two offset strips). **DOM contract:** the same values are mirrored as CSS custom properties (`--lg-observed`, etc.) consumed by `.legend` and SVG strokes. **WebGL contract:** `applyLineStyle(line, styleKey, mode)` sets material params and **scales dashSize/gapSize by the mode's display transform** (log2-compensated for trajectory modes, 1:1 for local linear frames) so dashes read identically at any radius. FINAL CANON gold override is a registered exception key (`CANON_OVERRIDE`), labeled in the legend — not a silent palette swap.

## 3. CHECKLIST VOTES

- **C1 — ACCEPT.** STEAL per M1's schema; fix seed to a valid uint32; trust/time scales resolved via C3/C4 below.
- **C2 — ACCEPT 0.186.0.** Newest stable at build; M1's three.js surface area is zero so the pin is the render master's call; addons I use are API-stable across 0.170→0.186.
- **C3 — ACCEPT TRUST 0..100, starts 50 (M1).** M3's 0–10 scale converts ×10; all meters normalize to 0..1 internally. One canonical field, no dual bookkeeping.
- **C4 — ACCEPT.** TIME = mission-day budget: `timeDays` consumed from 0, cap 4,200 + 120 contingency; M3's 120d becomes the named `timeContingency` reserve. UI shows remaining margin; spends route through `recompute()`.
- **C5 — ACCEPT.** Canon sim definitions are fixed (S1 timeline / S2 flyby / S3 meter / S4 cloud / S5 shepherd). M4 realigns; worksheet/checksum/hidden-burn/hearing are DOM scene interactions, not sim renames, and get no new WebGL modes without a filed geometry spec.
- **C6 — ACCEPT 11 scenes.** M3 remaps its spine 1:1 (S-8F becomes Scene 8's fail state, not a 12th screen). My camera rigs key on scene id — the count must be exact.
- **C7 — ACCEPT localStorage** as the persistence layer: STEAL snapshot + RUN records, versioned, schema-checked on load. Alternative to nothing — this is the only sane client-side choice.
- **C8 — ACCEPT mobile contract.** Canvas display-only on mobile; ALL sim inputs as DOM controls via `setModeState`; OrbitControls desktop-only. (My Round 1 attached controls in 5 modes unconditionally — corrected in §2.)
- **C9 — ACCEPT.** Build-time pre-rendered PNGs primary (M5 composite); runtime re-capture is the capture engine + dev/QA tool; sidecar JSON ships with every still. Captured in-browser, committed to `exhibits/` — no build step required.
- **C10 — ACCEPT 41×41 runtime LUT** from closed-form seeded formula, diegetic footnote "declassified targeting approximation." The 512-pt amber player line cannot resolve 41 vs 81; smaller texture, honest labeling, runtime-generated.

## 4. STILL MISSING

1. **Screen→sceneMode mapping table (B11)** — per screen: `{sceneMode | 'dom-only', cameraRig, requiredWebGLState}`. M3+M4 to produce; my `director.enter()` cannot be wired without it.
2. **`stateFromRun(run)`** — M1 to write the deterministic RUN→per-mode-state derivation. Without it, hearing replays (Exhibit 42B), A-2 branch rendering, and skipped-sim "NO DATA" states are **unrenderable** in WebGL.
3. **LUT contract** — `flyby.sample(aimDx, aimDy) → {rp, path}` signature, CPU-side, plus formula version tag for still sidecars (M1).
4. **Resource writer ownership** — ratify `recompute(STEAL)` as sole writer of derived `trust/timeDays/dv` fields; M3's earn/spend matrix calls it. Exact normalized meter fields.
5. **S5 numbers** — per-body nudge caps and the resonance-band definition (who computes in-band). I render; I don't invent game constants.
6. **S4 p→visual mapping** — `computeP(STEAL) → {p, tag}` from M1; I map p to cloud alpha/density, nothing more.
7. **FINAL CANON gold-override rule** — exact trigger (`STEAL.declass === 'FINAL_CANON'`) and legend labeling, so the truth grammar isn't silently broken.
8. **Stills commit flow** — who runs the QA capture page, PR/review gate for `exhibits/` PNGs, and the exact sidecar↔`exhibit-7b-plate-<nn>.png` filename linkage (M5+M2).
9. **`scenes.json` a11y labels** — per-scene `a11y` string for canvas `role="img"`/`aria-label` (M4/M5 to author).
10. **S2 stepper precision** — DOM aim stepper step size (propose 100 km, clamp ±5,000 km) so mobile and desktop converge on identical states.
